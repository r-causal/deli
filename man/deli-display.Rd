% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-summary.R
\name{deli-display}
\alias{deli-display}
\title{Display methods for deli estimators}
\arguments{
\item{x, object}{A fitted \code{MEstimator} or \code{GMMEstimator} object. Named \code{x} for
\code{print()} and \code{object} for \code{summary()}, because \code{\link[base:print]{base::print()}} and
\code{\link[base:summary]{base::summary()}} name their first argument that.}

\item{alpha}{Numeric significance level for the confidence limits reported by
\code{summary()}, between 0 and 1. Default \code{0.05} for 95\% limits.}

\item{subset}{Integer vector of parameter indices to display, or \code{NULL}
(default) to display all of them.}

\item{...}{Not used. Must be empty, so that a name neither method recognizes
is an error rather than silently ignored: a misspelled \code{alpha} would report
limits at the default width and a misspelled \code{subset} would display every
parameter, each while the call still read as the one that was meant.}
}
\value{
\itemize{
\item \code{print()}: its input, invisibly.
\item \code{summary()}: an object holding the estimates, standard errors, Z-scores,
confidence limits, P-values, and S-values, which prints as a table.
}
}
\description{
Methods for \code{\link[base:print]{base::print()}} and \code{\link[base:summary]{base::summary()}} so that a fitted estimator
shows its coefficients at the console and reports its parameter-level
inference as a table.
}
\details{
\code{print()} reports the parameter and observation counts followed by the
estimates, rounded to four decimal places. An estimator that has not been
through \code{\link[=estimate]{estimate()}} has no estimates to show, so it reports its parameter
count and says so.

\code{summary()} collects the estimates, their standard errors, Z-scores,
confidence limits, P-values, and S-values into one object, which prints as a
table with one row per parameter. The values are those
\code{\link[=deli-generics]{confint()}}, \code{\link[=z_scores]{z_scores()}}, \code{\link[=p_values]{p_values()}}, and \code{\link[=s_values]{s_values()}}
return individually, so \code{alpha} here means what it means there: \code{0.05} gives
95\% limits.

An over-identified \code{GMMEstimator} fit reports Hansen's J-statistic above the
table, with its degrees of freedom and its P-value, because it judges the fit
as a whole rather than any one parameter. See \code{\link[=GMMEstimator]{GMMEstimator()}} for what it
means and where its reference distribution holds. No other fit has one, so no
other output carries the line.

The \code{S} column reads \code{Inf} when a P-value underflows to exactly zero, for the
reason \code{\link[=s_values]{s_values()}} gives. The \code{P} column reports that same underflow as
\verb{<2e-16}: \code{\link[base:format.pval]{base::format.pval()}} stops printing digits below the \code{eps} it is
given, and the table gives it \code{2.2e-16}. \code{\link[generics:tidy]{generics::tidy()}} returns the
literal \code{0} instead.

\code{subset} restricts which parameters are displayed and nothing else. The
reported parameter count and every reported value are computed from the whole
fit, so displaying a subset of a stacked estimator is a way to read the
parameters of interest without the nuisance parameters, not a way to refit
without them. Row labels keep the names the full fit gave them.
}
\examples{
fit <- m_estimate(mpg ~ wt + hp, data = mtcars, .ee = ee_regression,
                  model = "linear")

fit

# `subset` is an argument of the method rather than of the fit, so showing it
# here means calling the generic by name.
print(fit, subset = 2:3)

summary(fit)

# `summary()` takes `subset` as well, alongside `alpha` for the width of the
# reported limits.
summary(fit, subset = 2:3, alpha = 0.1)

}
\seealso{
\link{deli-generics} for the accessors that return these quantities as
plain vectors and matrices, and \link{deli-tidiers} for the same results as a
data frame.
}
