% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{generate_weights}
\alias{generate_weights}
\title{Generate observation weights}
\usage{
generate_weights(n, weights = NULL)
}
\arguments{
\item{n}{Integer number of observations.}

\item{weights}{Numeric vector of weights, an n-row weight matrix, or \code{NULL}
for equal weights.}
}
\value{
A numeric vector of length \code{n}, or the supplied n-row matrix.
}
\description{
Returns observation weights. If \code{weights} is \code{NULL}, returns a vector of ones
(equal weighting). A numeric vector is validated against \code{n} and returned. A
weight matrix (one row per observation, one column per time interval, used by
\code{\link[=ee_plogit]{ee_plogit()}} for time-varying weights) is validated on its row count and
returned with its dimensions preserved; the column-count check is left to the
caller, which alone knows the number of intervals.
}
\keyword{internal}
