% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictions.R
\name{regression_predictions}
\alias{regression_predictions}
\title{Generate predicted values from a regression model}
\usage{
regression_predictions(X, theta, covariance, offset = NULL, alpha = 0.05)
}
\arguments{
\item{X}{Numeric n-by-p design matrix of covariate values for prediction.}

\item{theta}{Numeric vector of p estimated coefficients (from \code{coef(m)}).}

\item{covariance}{Numeric p-by-p covariance matrix (from \code{vcov(m)}).}

\item{offset}{Optional numeric vector of n offsets. Default \code{NULL}.}

\item{alpha}{Numeric significance level for confidence intervals.
Default \code{0.05} (95\% CIs).}
}
\value{
A data frame with n rows and columns: \code{predicted}, \code{variance},
\code{lower}, \code{upper}.
}
\description{
Computes predicted outcomes, their variance, and Wald-type confidence
intervals from estimated regression coefficients and their covariance
matrix. This is a post-processing utility meant to be used after
\code{\link[=MEstimator]{MEstimator()}} has been fitted.
}
\details{
No transformations are applied. For logistic models this returns log-odds
(not probabilities). Apply \code{\link[stats:plogis]{stats::plogis()}} for the probability scale, or
\code{\link[=inverse_logit]{inverse_logit()}} if the values feed a transform passed to \code{\link[=delta_method]{delta_method()}}
with \code{deriv_method = "exact"}.
}
\examples{
set.seed(1)
n <- 200
dat <- data.frame(x = rnorm(n), z = rbinom(n, 1, 0.5))
dat$y <- 1 + 0.5 * dat$x + 2 * dat$z + rnorm(n)

m <- m_estimate(y ~ x + z, data = dat, .ee = ee_regression, model = "linear")

# Predict along a small grid of x, holding z at 1. The columns of the grid
# must match the order of the coefficients, intercept first.
X_new <- cbind(1, x = c(-1, 0, 1), z = 1)
regression_predictions(X_new, theta = coef(m), covariance = vcov(m))

}
