% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/math-utils.R
\name{standard_normal_pdf}
\alias{standard_normal_pdf}
\title{Standard normal PDF}
\usage{
standard_normal_pdf(x)
}
\arguments{
\item{x}{Numeric value or vector of quantiles.}
}
\value{
Numeric density values.
}
\description{
Evaluates the probability density function of the standard normal
distribution.

This is the equivalent of \code{\link[stats:dnorm]{stats::dnorm()}} at the default mean and standard
deviation, and returns identical values for numeric input, except that it
carries derivatives. Exact differentiation (\code{deriv_method = "exact"})
propagates a tangent alongside each value, and \code{standard_normal_pdf()}
recognizes a tangent-carrying argument and applies the analytic rule itself,
\eqn{-x} times the density. \code{dnorm()} hands its argument to compiled code
without dispatching, and errors on such an argument. Use
\code{standard_normal_pdf()} inside estimating equations and inside transforms
passed to \code{\link[=delta_method]{delta_method()}}, and \code{dnorm()} for ordinary numeric work.
}
\section{Exact differentiation}{

\code{deriv_method = "exact"} is forward-mode automatic differentiation: it
replaces each value with an object carrying both the value and its
derivative. deli supports those objects through S3 methods: the \code{Ops},
\code{Math}, and \code{Summary} group generics, plus non-group methods such as \code{[},
\code{\%*\%}, and \code{t()}. \code{\link[=standard_normal_cdf]{standard_normal_cdf()}}, \code{\link[=standard_normal_pdf]{standard_normal_pdf()}},
\code{\link[=deli_polygamma]{deli_polygamma()}}, and \code{\link[=deli_digamma]{deli_digamma()}} recognize a tangent-carrying
argument themselves and apply their own analytic rule. Support within the
group generics is partial; see \code{vignette("getting-started")} for the
operations deli differentiates.

\code{plogis()}, \code{qlogis()}, \code{pnorm()}, \code{dnorm()}, and \code{psigamma()} take none of
these paths. Each hands its argument straight to compiled code through
\code{.Call()} or \code{.Internal()} without dispatching, so the tangent reaches C code
that requires a plain number and the call stops with a
\verb{non-numeric argument to mathematical function} error.

Each deli utility below returns the same values as its base R counterpart for
numeric input. What separates them is whether the counterpart survives exact
mode:\tabular{lll}{
   deli function \tab base R counterpart \tab base R under \code{deriv_method = "exact"} \cr
   \code{\link[=inverse_logit]{inverse_logit()}} \tab \code{\link[stats:plogis]{stats::plogis()}} \tab errors \cr
   \code{\link[=logit]{logit()}} \tab \code{\link[stats:qlogis]{stats::qlogis()}} \tab errors \cr
   \code{\link[=standard_normal_cdf]{standard_normal_cdf()}} \tab \code{\link[stats:pnorm]{stats::pnorm()}} \tab errors \cr
   \code{\link[=standard_normal_pdf]{standard_normal_pdf()}} \tab \code{\link[stats:dnorm]{stats::dnorm()}} \tab errors \cr
   \code{\link[=deli_polygamma]{deli_polygamma()}} \tab \code{\link[base:psigamma]{base::psigamma()}} \tab errors \cr
   \code{\link[=deli_digamma]{deli_digamma()}} \tab \code{\link[base:digamma]{base::digamma()}} \tab works \cr
   \code{\link[=identity_transform]{identity_transform()}} \tab \code{\link[base:identity]{base::identity()}} \tab works \cr
}


For the first five rows, use the deli function inside estimating equations
and inside transforms passed to \code{\link[=delta_method]{delta_method()}}, and the base R function
everywhere else: simulating data, post-fit display, plain numeric work. For
the last two rows the base R function is usable everywhere, because
\code{digamma()} is a \code{Math} group member with a tangent rule and \code{identity()}
passes its argument through untouched.

The polygamma row is the one place where the arguments do not line up.
\code{deli_polygamma(n, x)} takes the derivative order first and
\code{psigamma(x, deriv = n)} takes it second, so a positional substitution
between the two computes a different quantity and raises no error.
}

\examples{
standard_normal_pdf(0)
standard_normal_pdf(c(-1, 0, 1))

# The same values as the base R counterpart
all.equal(standard_normal_pdf(c(-1, 0, 1)), dnorm(c(-1, 0, 1)))

m <- m_estimate(
  vs ~ mpg,
  data = mtcars,
  .ee = ee_glm,
  distribution = "binomial",
  link = "probit"
)

# Variance of the marginal effect of mpg at mpg = 20, the standard normal
# density at the linear predictor times the coefficient. Writing
# `dnorm(theta[1] + theta[2] * 20)` here instead would error, because exact
# differentiation hands the transform a tangent-carrying argument.
delta_method(
  m,
  transform = function(theta) {
    standard_normal_pdf(theta[1] + theta[2] * 20) * theta[2]
  },
  deriv_method = "exact"
)

}
\seealso{
\code{\link[stats:dnorm]{stats::dnorm()}}, the base R equivalent for ordinary numeric work,
and \code{\link[=standard_normal_cdf]{standard_normal_cdf()}} for the distribution function.
}
