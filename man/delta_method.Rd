% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandwich.R
\name{delta_method}
\alias{delta_method}
\title{Delta method for variance of transformed parameters}
\usage{
delta_method(
  object,
  transform,
  covariance = NULL,
  deriv_method = "capprox",
  dx = 1e-09,
  ...
)
}
\arguments{
\item{object}{A fitted \code{MEstimator} object, or a numeric vector of
parameter estimates.}

\item{transform}{Function that takes \code{theta} and returns a numeric vector.}

\item{covariance}{Numeric covariance matrix (only used when \code{object} is a
numeric vector).}

\item{deriv_method}{Character string for the derivative method used to build
the Jacobian of \code{transform}. One of \code{"capprox"} (central difference),
\code{"fapprox"} (forward difference), \code{"bapprox"} (backward difference), or
\code{"exact"} (forward-mode automatic differentiation). Default \code{"capprox"}.}

\item{dx}{Numeric step size for the finite-difference methods; ignored when
\code{deriv_method = "exact"}. Default \code{1e-9}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A covariance matrix for \code{transform(theta)}.
}
\description{
Computes the variance-covariance matrix for a transformation of parameters
using the Delta Method:
\deqn{Var[g(\theta)] \approx G \Sigma G^T}
where \eqn{G} is the Jacobian of \eqn{g} and \eqn{\Sigma} is the
covariance matrix of \eqn{\theta}.
}
