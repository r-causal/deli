% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MEstimator.R
\name{MEstimator}
\alias{MEstimator}
\title{M-Estimator}
\usage{
MEstimator(stacked_equations, init, subset = NULL, finite_correction = NULL)
}
\arguments{
\item{stacked_equations}{A function that takes a numeric vector \code{theta} and
returns a p-by-n matrix of estimating equation contributions, where p is
the number of parameters and n is the number of observations. Row names on
that matrix name the parameters when \code{init} has none and every parameter is
labeled; see \code{\link[=estimate]{estimate()}}.}

\item{init}{Numeric vector of initial parameter values for the root-finding
algorithm. Names on it label the parameters and take precedence over the
row names of \code{stacked_equations}.}

\item{subset}{Integer vector of parameter indices to solve for, or \code{NULL}
(default) to solve for all parameters. Indices are 1-based; parameters not
listed are held fixed at their \code{init} values while the rest are solved. The
equations outside the subset are set aside along with the parameters they
estimate, so the subset parameters are the root of the subset equations
alone and the rest of the stack has no say in where they land: give a
three-equation linear regression stack \code{subset = 1L} and the intercept comes
back as the mean of the response less what the slopes held at their \code{init}
values account for, because the first equation on its own is the estimating
equation for a mean. Held at zero, which is what an unset \code{init} usually
means, they account for nothing and the intercept is the mean of the
response itself. \code{\link[=GMMEstimator]{GMMEstimator()}} and \code{\link[=gmm_estimate]{gmm_estimate()}}
read the argument differently, since the GMM objective sums every equation
whether the subset lists it or not, so the same stack and the same \code{subset}
give the two different values. The variance estimator ignores \code{subset}.}

\item{finite_correction}{Character string for finite-sample correction
(e.g., \code{"HC1"}), or \code{NULL} (default) for no correction.}
}
\value{
An \code{MEstimator} S7 object. Call \code{\link[=estimate]{estimate()}} to solve the
estimating equations and compute the sandwich variance.
}
\description{
S7 class for M-estimation via solving estimating equations with empirical
sandwich variance estimation.
}
\examples{
# Estimating equations for the mean
y <- c(1, 2, 3, 4, 5)
psi <- function(theta) {
  matrix(y - theta[1], nrow = 1)
}
m <- MEstimator(stacked_equations = psi, init = 0) |>
  estimate()
coef(m)
summary(m)
}
