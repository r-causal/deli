% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-basic.R
\name{ee_mean_geometric}
\alias{ee_mean_geometric}
\title{Estimating equation for the geometric mean}
\usage{
ee_mean_geometric(theta, y, weights = NULL, log_theta = TRUE)
}
\arguments{
\item{theta}{Numeric vector of length 1.}

\item{y}{Numeric vector of positive observed values.}

\item{weights}{Optional numeric vector of weights. Default \code{NULL}.}

\item{log_theta}{Logical. If \code{TRUE} (default), internally log-transforms
theta. Default \code{TRUE}.}
}
\value{
A 1-by-n matrix.
}
\description{
Returns a 1-by-n matrix of estimating equation contributions for the
geometric mean. When \code{log_theta = TRUE} (default), solves
\eqn{\log(Y_i) - \log(\theta)}. When \code{log_theta = FALSE}, solves
\eqn{\log(Y_i) - \theta} where \eqn{\theta} is the log of the geometric
mean.
}
\examples{
y <- c(1, 2, 3, 1, 4, 5, 3, 2, 6, 7)
psi <- function(theta) ee_mean_geometric(theta, y = y)
m <- MEstimator(stacked_equations = psi, init = 1) |>
  estimate()
coef(m)

}
