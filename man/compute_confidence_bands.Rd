% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandwich.R
\name{compute_confidence_bands}
\alias{compute_confidence_bands}
\title{Compute confidence bands from theta and covariance}
\usage{
compute_confidence_bands(
  theta,
  covariance,
  alpha = 0.05,
  method = "supt",
  n_draws = 100000L,
  seed = NULL
)
}
\arguments{
\item{theta}{Numeric parameter vector.}

\item{covariance}{Numeric covariance matrix.}

\item{alpha}{Significance level. Default \code{0.05}.}

\item{method}{\code{"supt"} or \code{"bonferroni"}. Default \code{"supt"}.}

\item{n_draws}{Number of MVN draws for sup-t. Default \code{1e5}. See
\code{\link[=confidence_bands]{confidence_bands()}} for why this differs from Python's \code{1e6} estimator
default and how to match it.}

\item{seed}{RNG seed. Default \code{NULL}.}
}
\value{
A p-by-2 matrix with columns \code{"lower"} and \code{"upper"}. Rows take
their names from \code{theta}, when it has any.
}
\description{
Compute confidence bands from theta and covariance
}
\examples{
\dontshow{if (requireNamespace("MASS", quietly = TRUE)) withAutoprint(\{ # examplesIf}
fit <- m_estimate(mpg ~ wt + hp, data = mtcars, .ee = ee_regression,
                  model = "linear")

# Bands from the estimates and covariance alone, without the fitted object
compute_confidence_bands(coef(fit), covariance = vcov(fit),
                         method = "supt", seed = 1)
\dontshow{\}) # examplesIf}
}
