% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline.R
\name{additive_design_matrix}
\alias{additive_design_matrix}
\title{Build an additive design matrix for GAMs}
\usage{
additive_design_matrix(X, specifications, return_penalty = FALSE)
}
\arguments{
\item{X}{Numeric matrix (n-by-b) of input covariates.}

\item{specifications}{A list of length \code{b} (number of columns in \code{X}).
Each element is either \code{NULL} (no spline for that column) or a list with:
\describe{
\item{knots}{Numeric vector of knot locations (required).}
\item{natural}{Logical, generate restricted splines? Default \code{TRUE}.}
\item{power}{Numeric power for spline. Default \code{3}.}
\item{penalty}{Numeric penalty for spline terms. Default \code{0}.}
\item{normalized}{Logical, normalize spline terms? Default \code{FALSE}.}
}}

\item{return_penalty}{Logical. If \code{TRUE}, return a list with both the
design matrix and the penalty vector. Default \code{FALSE}.}
}
\value{
If \code{return_penalty = FALSE}, a numeric matrix. If \code{TRUE}, a list
with elements \code{X} (the design matrix) and \code{penalty} (numeric vector).
}
\description{
Constructs the expanded design matrix for generalized additive models by
appending spline basis terms according to per-column specifications. Each
column in \code{X} keeps its linear term; columns with a non-\code{NULL} specification
get additional spline basis columns appended.
}
\examples{
set.seed(42)
X <- cbind(rnorm(50), rnorm(50))

# The first column stays linear; the second also gets penalized splines
specs <- list(NULL, list(knots = c(-1, 0, 1), penalty = 5))
out <- additive_design_matrix(X, specs, return_penalty = TRUE)

# Linear terms are unpenalized, spline terms carry the requested penalty
out$penalty

dim(out$X)

}
