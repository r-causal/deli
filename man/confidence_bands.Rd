% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandwich.R
\name{confidence_bands}
\alias{confidence_bands}
\title{Confidence bands for parameter vectors}
\usage{
confidence_bands(
  object,
  alpha = 0.05,
  method = "supt",
  n_draws = 100000L,
  seed = NULL,
  subset = NULL,
  covariance = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted \code{MEstimator} object, or a numeric vector of
parameter estimates.}

\item{alpha}{Numeric significance level, between 0 and 1. Default \code{0.05}.}

\item{method}{Character string. \code{"supt"} (default) for supremum-t or
\code{"bonferroni"} for Bonferroni correction.}

\item{n_draws}{Integer number of MVN draws for the sup-t method.
Default \code{1e5}. The critical value is a Monte Carlo estimate of a fixed
sup-t quantile, and at \code{1e5} draws the band half-width varies by roughly
0.2\% across independent draws, which is negligible relative to the band
width. Python's estimator method defaults to \code{1e6}; both defaults target
the same quantity and agree to within Monte Carlo error, so \code{deli} keeps
the smaller default for faster computation. Set \code{n_draws = 1e6} to match
Python's estimator default. Note that seeded band values are not
reproducible across the two languages because the MVN samplers differ.}

\item{seed}{Integer seed for reproducibility. Default \code{NULL}.}

\item{subset}{Integer vector of parameter indices to compute bands for.
Default \code{NULL} (all parameters).}

\item{covariance}{Numeric covariance matrix (only when \code{object} is numeric).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A p-by-2 matrix with columns \code{"lower"} and \code{"upper"}.
}
\description{
Computes simultaneous confidence bands that provide coverage for the
entire parameter vector, adjusting for multiple comparisons. The formula is:
\deqn{\hat{\theta} \pm \hat{c}_{\alpha/2} \times \widehat{SE}(\hat{\theta})}
where \eqn{\hat{c}} is the adjusted critical value.
}
