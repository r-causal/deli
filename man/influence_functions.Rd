% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/influence.R
\name{influence_functions}
\alias{influence_functions}
\title{Influence functions for M-Estimator}
\usage{
influence_functions(object, allow_pinv = TRUE, ...)
}
\arguments{
\item{object}{A fitted \code{MEstimator} object (after calling \code{\link[=estimate]{estimate()}}).}

\item{allow_pinv}{Logical. Use pseudo-inverse if bread is singular? Default
\code{TRUE}.}

\item{...}{Not used. Must be empty, so a name that is not one of the
documented arguments is an error rather than silently ignored.}
}
\value{
An n-by-p matrix of influence function values, where n is the
number of observations and p is the number of parameters. Columns are named
for the parameters, as in \code{\link[=deli-generics]{coef()}}.
}
\description{
Computes the influence function values for individual observations:
\deqn{IF(O_i; \theta) = B_n(\theta)^{-1} \psi(O_i; \theta)}

This function mirrors \code{m.influence_functions()} in Python delicatessen, so
code translated from Python can keep its shape. There is no base R accessor
for influence function values, so this is the interface for them in deli as
well.
}
\examples{
fit <- m_estimate(mpg ~ wt + hp, data = mtcars, .ee = ee_regression,
                  model = "linear")

# One row per observation, showing its contribution to each estimate
head(influence_functions(fit))

}
