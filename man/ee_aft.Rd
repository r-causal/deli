% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-survival.R
\name{ee_aft}
\alias{ee_aft}
\title{Estimating equation for accelerated failure time models}
\usage{
ee_aft(theta, X, time, event, distribution, weights = NULL, offset = NULL)
}
\arguments{
\item{theta}{Numeric vector of length \code{b + 1} (or \code{b} for exponential).
The first \code{b} elements are regression coefficients and the last element
is \code{log(1/sigma)} (the log-inverse scale).}

\item{X}{Numeric n-by-b design matrix (should include intercept column).}

\item{time}{Numeric vector of n observed (possibly censored) times.}

\item{event}{Numeric vector of n event indicators (1 = event, 0 = censored).}

\item{distribution}{Character string: \code{"weibull"}, \code{"exponential"},
\code{"log-logistic"}, or \code{"log-normal"}.}

\item{weights}{Optional numeric vector of n weights. Default \code{NULL}.}

\item{offset}{Optional numeric vector of n offsets. Default \code{NULL}.}
}
\value{
A p-by-n matrix where p is the number of parameters.
}
\description{
Returns a p-by-n matrix of estimating equation contributions for
accelerated failure time (AFT) models. Supports Weibull, exponential,
log-logistic, and log-normal distributions.
}
\details{
The AFT model uses the parameterization where
\eqn{Z_i = (\log(t_i) - X_i \beta) / \sigma} and the last element of
theta is \eqn{\log(1/\sigma)}.
}
