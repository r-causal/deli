% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.R
\name{deli-tidiers}
\alias{deli-tidiers}
\title{Broom tidiers for deli estimators}
\arguments{
\item{x}{A fitted \code{MEstimator} or \code{GMMEstimator} object.}

\item{conf.int}{Logical. Include confidence intervals? Default \code{FALSE}.}

\item{conf.level}{Numeric confidence level for intervals. Default \code{0.95}.}

\item{...}{Additional arguments (unused).}
}
\value{
\itemize{
\item \code{tidy()}: A data.frame with columns \code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{p.value}, \code{s.value}. If \code{conf.int = TRUE}, also includes
\code{conf.low} and \code{conf.high}.
\item \code{glance()}: A single-row data.frame with model-level summaries.
}
}
\description{
\code{\link[generics:tidy]{generics::tidy()}} and \code{\link[generics:glance]{generics::glance()}} methods for \code{MEstimator} and
\code{GMMEstimator} objects. These allow deli results to flow into tidyverse
pipelines. Requires the \pkg{generics} package (or \pkg{broom}) to be
loaded.
}
\examples{
fit <- m_estimate(mpg ~ wt + hp, data = mtcars, .ee = ee_regression,
                  model = "linear")

# Attaching generics or broom makes the shorter tidy(fit) and glance(fit)
# calls work as well
generics::tidy(fit, conf.int = TRUE)

generics::glance(fit)

}
