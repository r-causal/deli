% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.R
\name{deli-tidiers}
\alias{deli-tidiers}
\title{Broom tidiers for deli estimators}
\arguments{
\item{x}{A fitted \code{MEstimator} or \code{GMMEstimator} object.}

\item{conf.int}{Logical. Include confidence intervals? Default \code{FALSE}.}

\item{conf.level}{Numeric confidence level for intervals. Default \code{0.95}.}

\item{...}{Not used. \code{tidy()} requires them to be empty, so that a misspelled
\code{conf.int} or \code{conf.level} is an error rather than a table silently
returned without intervals or at the default level. \code{glance()} has no
optional argument for a wrong name to displace and ignores them.}
}
\value{
\itemize{
\item \code{tidy()}: A data.frame with columns \code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{p.value}, \code{s.value}. If \code{conf.int = TRUE}, also includes
\code{conf.low} and \code{conf.high}. A \code{p.value} that underflows to exactly zero is
reported as \code{0} alongside an infinite \code{s.value}; see \code{\link[=s_values]{s_values()}}.
\item \code{glance()}: A single-row data.frame with model-level summaries: \code{nobs},
\code{npar}, \code{estimator}, \code{finite_correction}, and the Hansen J-statistic of an
over-identified GMM fit in \code{j_statistic}, \code{j_df} and \code{j_p_value}. The three
J columns are present on every fit and hold the typed missing value of
their own type where there is no such statistic, which is every
M-estimation fit and every just-identified or \code{subset} GMM fit; see
\code{\link[=GMMEstimator]{GMMEstimator()}} for what the statistic reads and where it is left unset.
}
}
\description{
\code{tidy()} and \code{glance()} methods for \code{MEstimator} and \code{GMMEstimator} objects.
These allow deli results to flow into tidyverse pipelines.
}
\examples{
fit <- m_estimate(mpg ~ wt + hp, data = mtcars, .ee = ee_regression,
                  model = "linear")

tidy(fit, conf.int = TRUE)

glance(fit)

}
\seealso{
\link{deli-augment}, the third broom generic, which returns the
observation-level fitted values, intervals, and residuals, and \link{reexports}
for the generics themselves, which deli re-exports so that \code{tidy()} and
\code{glance()} resolve with deli alone attached.
}
