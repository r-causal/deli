% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{deli-augment}
\alias{deli-augment}
\title{Augment data with predictions from a fitted deli estimator}
\arguments{
\item{x}{A fitted \code{MEstimator} or \code{GMMEstimator} object made with the formula
interface (after calling \code{\link[=estimate]{estimate()}}).}

\item{newdata}{A data frame of covariate values to predict at, or \code{NULL}
(default) to report the model frame the fit was built from. That frame
holds the variables the formula named, so a transformed term appears as the
column the transformation produced and a column of the fitting data the
formula did not name is not there.}

\item{type.predict}{Character string. \code{"link"} (default) puts \code{.fitted} and
its interval on the scale of the linear predictor; \code{"response"} puts them
on the scale of the response.}

\item{conf.level}{Numeric confidence level for \code{.lower} and \code{.upper}.
Default \code{0.95}.}

\item{...}{Not used. Must be empty, so that a name that is not one of the
documented arguments is an error rather than silently ignored. A
misspelled \code{newdata} would otherwise augment the fitted rows while the
caller believed they had asked for rows of their own.}
}
\value{
A data frame: the model frame, or \code{newdata}, followed by the columns
\code{.fitted}, \code{.se.fit}, \code{.lower}, \code{.upper}, and \code{.resid}. The last is absent
when \code{newdata} is supplied.
}
\description{
A \code{\link[generics:augment]{generics::augment()}} method for \code{MEstimator} and \code{GMMEstimator} objects
fitted through the formula interface. It returns the model frame the fit was
built from, or \code{newdata} when supplied, with the fitted values, their
standard errors, a Wald confidence interval, and the residuals as columns
beside it.
}
\details{
The columns added are \code{.fitted}, \code{.se.fit}, \code{.lower}, \code{.upper}, and, when
\code{newdata} is not supplied, \code{.resid}. They are exactly what
\code{\link[=deli-predict]{predict()}} and \code{\link[=deli-generics]{residuals()}} return for the
same fit, so \code{.fitted} is \code{predict()}, \code{.lower} and \code{.upper} are
\code{predict(interval = "confidence")}, and \code{.resid} is \code{residuals()}.

\code{.fitted} is on the link scale by default, matching both
\code{\link[=deli-predict]{predict()}} and \code{broom::augment()} on a \code{glm}, and
\code{type.predict = "response"} puts it and its interval on the scale of the
response. \code{.resid} is the response residual either way, since a residual
measured against a linear predictor would be a response minus a quantity the
response is not measured in.

Rows the fit dropped for missing data are not reported, so the result has one
row per \code{\link[=deli-generics]{nobs()}} and its row names are those of the retained
rows. A \code{newdata} row with a missing value is kept, with \code{NA} in the added
columns, so that the result lines up with the rows handed in.

\code{augment()} covers the estimating equations whose linear predictor
\code{\link[=deli-predict]{predict()}} forms, and refuses the same fits with the same
reasons; see \link{deli-predict}. It has no counterpart to that method's \code{times}
argument: a survival measure is one value per row of the data and time rather
than one per row, so the predictions do not go beside the data as columns.
}
\examples{
fit <- m_estimate(mpg ~ wt + hp, data = mtcars, .ee = ee_regression,
                  model = "linear")

# Attaching generics or broom makes the shorter augment(fit) call work
# as well
head(generics::augment(fit))

# New covariate patterns come back with no residual column, since they carry
# no response to residualize against.
generics::augment(fit, newdata = data.frame(wt = c(2, 3, 4), hp = 110))

}
\seealso{
\link{deli-predict} for the predictions themselves and the equations they
are available for, and \link{deli-tidiers} for the parameter-level and
model-level summaries.
}
