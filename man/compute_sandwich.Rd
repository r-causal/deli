% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandwich.R
\name{compute_sandwich}
\alias{compute_sandwich}
\title{Compute the empirical sandwich variance estimator}
\usage{
compute_sandwich(
  stacked_equations,
  theta,
  deriv_method = "capprox",
  dx = 1e-09,
  allow_pinv = TRUE,
  finite_correction = NULL,
  summed_equations = NULL,
  check_summed_equations = TRUE
)
}
\arguments{
\item{stacked_equations}{A function that takes a numeric vector \code{theta} and
returns a p-by-n matrix of estimating equation contributions, where p is
the number of parameters and n is the number of observations. A list of one
element per equation, each holding that equation's contributions across the
observations, is accepted as well. \code{\link[=estimate]{estimate()}} has no support for that
shape, so an estimating function written in it reaches a variance through
this entry point and a Jacobian through \code{\link[=compute_bread]{compute_bread()}}. The list form
holds exactly one element per parameter, each of one length, so an
over-identified system reaches this function as a matrix rather than as a
list.}

\item{theta}{Numeric vector of parameter estimates. This function assumes
\code{theta} is the root of \code{stacked_equations}; it does not solve for it.}

\item{deriv_method}{Character string selecting the method used to build the
bread Jacobian. One of \code{"capprox"} (central difference), \code{"fapprox"}
(forward difference), \code{"bapprox"} (backward difference), or \code{"exact"}
(forward-mode automatic differentiation). Default \code{"capprox"}.

This default differs from Python delicatessen, whose \code{compute_sandwich}
defaults to \code{"approx"}, a forward difference computed through SciPy's
\code{approx_fprime}. deli does not replicate the SciPy \code{"approx"} path; its
\code{"fapprox"} is the hand-implemented forward difference. Code ported across
the two libraries should set \code{deriv_method} explicitly rather than rely on
the default.}

\item{dx}{Numeric step size for the finite-difference methods; ignored when
\code{deriv_method = "exact"}. A small value is recommended, since large steps
can give poor approximations. Default \code{1e-9}. Must be a single positive
finite number, which is checked whichever \code{deriv_method} is in force. The
step is absolute and is floored at the floating-point resolution of each
estimate, so a large parameter magnitude cannot silently reduce it to
nothing; see \code{\link[=approx_differentiation]{approx_differentiation()}}.}

\item{allow_pinv}{Logical. When \code{TRUE} (default), the Moore-Penrose
pseudo-inverse is used if the bread matrix has no inverse, which is the
case for the rectangular bread of an over-identified system as well as for
a singular square one; when \code{FALSE}, a bread with no inverse raises an
error.}

\item{finite_correction}{Character string or \code{NULL}. Finite-sample correction
applied to the meat matrix. \code{NULL} (default) applies no correction; \code{"HC1"}
rescales the meat by \eqn{n / (n - p)}, where p is the number of parameters.}

\item{summed_equations}{A function that takes a numeric vector \code{theta} and
returns the length-p vector of row sums of \code{stacked_equations} at \code{theta},
or \code{NULL} (default) to derive those sums from the full p-by-n return.

The bread is the Jacobian of the summed estimating equations, so each of the
one or two perturbed evaluations it makes per parameter is reduced to one
value per equation as soon as it is built. Deriving the reduction builds the
whole p-by-n matrix 2p times for arithmetic that is linear in it; an
estimating function whose sums have a closed form, such as the
\eqn{X^T r} of a regression score, can supply them and hand the bread only
what it uses. The meat is unaffected either way, since it needs the
per-observation contributions and takes the one full evaluation it always
took.

Under \code{deriv_method = "exact"} the reduction is called with a
tangent-carrying \code{theta}, so it must be written in operations that carry
derivatives: \code{t(X) \%*\% r} does, and \code{\link[base:crossprod]{base::crossprod()}} does not. See
\code{\link[=auto_differentiation]{auto_differentiation()}} for which operations carry a tangent and where.

An argument that is neither \code{NULL} nor a function raises an error carrying
the class \code{deli_summed_equations_error}, whatever \code{check_summed_equations}
says. So does a return at \code{theta} that is not numeric, or one holding fewer
values than there are parameters, both of which \code{\link[=compute_bread]{compute_bread()}} reads
before it differentiates anything. That the return holds exactly one value
per estimating equation is read by the comparison below, since only a call
that has evaluated the estimating functions knows how many of them there
are.}

\item{check_summed_equations}{Logical. When \code{TRUE} (default) and
\code{summed_equations} was supplied, its value at \code{theta} is compared against
the row sums of the one full evaluation the meat is built from, and a
disagreement raises an error carrying the class
\code{deli_summed_equations_disagree}. The comparison costs one call to
\code{summed_equations} and one reduction of a matrix the call already holds.
Set it to \code{FALSE} to skip the comparison, which leaves a reduction that sums
some other system to return a matrix with the shape of a covariance and no
claim to be one.

The comparison is made at \code{theta}, which the caller states is the root, so
both quantities are at rounding there. That is enough to catch a reduction
of some other system and not enough to catch a reduction that is a multiple
of the right one, which agrees at a root and yields that multiple of the
right bread.}
}
\value{
A p-by-p covariance matrix on the asymptotic scale. The bread and
meat are each divided by n internally, so the returned matrix is the
variance that corresponds to the standard deviation. Dividing it by the
number of observations gives the standard-error-scale variance, whose
square-rooted diagonal is the vector of standard errors.

A covariance matrix is the only thing this function returns. Where the
bread has no inverse, and so no sandwich can be assembled from it, the call
raises an error carrying the class \code{deli_bread_not_invertible} rather than
returning something that has to be tested for. That covers a bread holding
\code{NA}, and, under \code{allow_pinv = FALSE}, a rectangular or a rank-deficient
one. \code{\link[=estimate]{estimate()}} makes the other choice from the same matrices: a fit whose
bread holds \code{NA} warns and comes back with no variance, since it still
carries the estimates.
}
\description{
Computes the empirical sandwich variance estimator directly from a set of
estimating equations and a vector of parameter estimates. Unlike
\code{\link[=MEstimator]{MEstimator()}}, this function does not solve for the parameters; it assumes
that \code{theta} is already the root of the estimating equations and only
assembles the sandwich covariance at that point.
}
\details{
The sandwich is built from a bread matrix and a meat matrix. The bread is the
negative Jacobian of the summed estimating equations, and the meat is the
cross-product of the equation evaluations. Each is scaled by \code{1/n} internally,
so the returned matrix is on the asymptotic scale (see \code{Value}). The bread
Jacobian is obtained either by finite differences or by forward-mode
automatic differentiation.
}
\examples{
# A generic data set for estimating a mean and variance
y <- c(1, 2, 4, 1, 2, 3, 1, 5, 2)

# The mean and variance are the roots of ee_mean_variance, so they can be
# computed directly rather than solved for
theta <- c(mean(y), stats::var(y) * (length(y) - 1) / length(y))

# Wrap the built-in estimating equation as a function of theta alone
psi <- function(theta) ee_mean_variance(theta, y = y)

# compute_sandwich() returns the asymptotic-scale variance, so dividing by
# n puts it on the standard-error scale
sandwich <- compute_sandwich(psi, theta = theta) / length(y)
sandwich

# The diagonal square roots are the standard errors
sqrt(diag(sandwich))

# The bread only ever needs the sums of the estimating equations, so an
# equation whose sums have a closed form can supply them directly
summed <- function(theta) {
  c(
    sum(y) - length(y) * theta[1],
    sum((y - theta[1])^2) - length(y) * theta[2]
  )
}
compute_sandwich(psi, theta = theta, summed_equations = summed) / length(y)

}
\references{
Boos DD, & Stefanski LA. (2013). M-estimation (estimating equations). In
Essential Statistical Inference (pp. 297-337). Springer, New York, NY.
}
\seealso{
\code{\link[=MEstimator]{MEstimator()}} and \code{\link[=GMMEstimator]{GMMEstimator()}}, which solve for \code{theta} and
report this variance internally, and \code{\link[=delta_method]{delta_method()}} for the variance of a
transformation of the parameters.
}
