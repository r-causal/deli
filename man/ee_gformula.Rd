% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-causal.R
\name{ee_gformula}
\alias{ee_gformula}
\title{Estimating equations for the g-formula (g-computation)}
\usage{
ee_gformula(theta, y, X, X1, X0 = NULL, force_continuous = FALSE)
}
\arguments{
\item{theta}{Numeric vector. If \code{X0 = NULL}, length is \code{1 + p} (causal
mean + regression coefficients). If \code{X0} is provided, length is
\code{3 + p} (ACE, mean under X1, mean under X0, regression coefficients).}

\item{y}{Numeric vector of n observed outcomes.}

\item{X}{Numeric n-by-p design matrix (observed data).}

\item{X1}{Numeric n-by-p design matrix under action plan 1.}

\item{X0}{Optional n-by-p design matrix under action plan 0.
Default \code{NULL}.}

\item{force_continuous}{Logical. Force linear regression even when \code{y}
is binary? Default \code{FALSE}.}
}
\value{
A matrix of estimating equation contributions.
}
\description{
Returns a stacked set of estimating equations for the g-formula. When
\code{X0 = NULL}, estimates a single causal mean under the plan encoded by
\code{X1}. When \code{X0} is provided, estimates the average causal effect
(difference between two plans).
}
\examples{
# A binary treatment, two confounders, and a continuous outcome whose true
# average causal effect is 1.5.
set.seed(42)
n <- 1000
W1 <- rnorm(n)
W2 <- rbinom(n, 1, 0.4)
A <- rbinom(n, 1, inverse_logit(-0.5 + 0.5 * W1 + 0.3 * W2))
Y <- 2 + 1.5 * A + W1 - 0.5 * W2 + rnorm(n)

X <- cbind(1, A, W1, W2) # Observed design matrix
X1 <- cbind(1, 1, W1, W2) # Everyone treated
X0 <- cbind(1, 0, W1, W2) # Everyone untreated

psi <- function(theta) ee_gformula(theta, y = Y, X = X, X1 = X1, X0 = X0)

# theta holds the average causal effect, the mean under treatment, and the
# mean under no treatment, followed by the four outcome model coefficients.
m <- MEstimator(stacked_equations = psi, init = rep(0, 7)) |>
  estimate()
coef(m)[1:3]

}
