% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictions.R
\name{survival_predictions}
\alias{survival_predictions}
\title{Generate predicted survival measures from a parametric survival model}
\usage{
survival_predictions(
  times,
  theta,
  covariance,
  distribution,
  measure = "survival",
  alpha = 0.05,
  deriv_method = "capprox",
  dx = 1e-09
)
}
\arguments{
\item{times}{Numeric vector of time points for prediction.}

\item{theta}{Numeric vector of estimated parameters from
\code{ee_survival_model}.}

\item{covariance}{Numeric covariance matrix from \code{vcov(m)}.}

\item{distribution}{Character string matching the distribution used in
\code{ee_survival_model}: \code{"exponential"}, \code{"weibull"}, or \code{"gompertz"}. Any
other value is an error.}

\item{measure}{Character string: \code{"survival"}, \code{"risk"}, \code{"density"},
\code{"hazard"}, or \code{"cumulative_hazard"}. Default \code{"survival"}.}

\item{alpha}{Numeric significance level. Default \code{0.05}.}

\item{deriv_method}{Character string for the derivative method used to build
the delta-method Jacobian. One of \code{"capprox"} (central difference),
\code{"fapprox"} (forward difference), \code{"bapprox"} (backward difference), or
\code{"exact"} (forward-mode automatic differentiation). Default \code{"capprox"}.
Python Delicatessen uses exact differentiation internally; pass
\code{deriv_method = "exact"} to reproduce it with exact derivatives and no
step-size tuning. See \code{\link[=delta_method]{delta_method()}}.}

\item{dx}{Numeric step size for the finite-difference methods; ignored when
\code{deriv_method = "exact"}. Default \code{1e-9}. The step is absolute and is
floored at the floating-point resolution of each estimate, so a large
parameter magnitude cannot silently reduce it to nothing; see
\code{\link[=approx_differentiation]{approx_differentiation()}}.}
}
\value{
A data frame with columns: \code{time}, \code{predicted}, \code{variance},
\code{lower}, \code{upper}.
}
\description{
Computes predicted survival analysis measures and point-wise confidence
intervals using the delta method. Meant to be used after fitting
\code{\link[=ee_survival_model]{ee_survival_model()}} with \code{\link[=MEstimator]{MEstimator()}}.
}
\section{Gompertz distribution}{

The Gompertz survival and hazard follow the \code{\link[=ee_survival_model]{ee_survival_model()}}
parameterization,

\deqn{S(t) = \exp\left(-\frac{\lambda}{\gamma}\left(e^{\gamma t} - 1\right)
  \right), \qquad h(t) = \lambda e^{\gamma t}.}

This is a deliberate divergence from Python Delicatessen, whose
\code{survival_predictions} branches only on the exponential distribution and
otherwise applies the Weibull formulas, so a \code{"gompertz"} request there
silently returns Weibull values.
}

\examples{
\dontshow{if (requireNamespace("nleqslv", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# Weibull survival times for 45 women with breast cancer, with no covariates.
# The default rootSolve solver does not converge here, so nleqslv is used.
psi <- function(theta) {
  ee_survival_model(
    theta,
    time = breast_cancer$times,
    event = breast_cancer$delta,
    distribution = "weibull"
  )
}
m <- m_estimate(
  stacked_equations = psi,
  init = c(0.1, 0.1),
  solver = "nleqslv"
)

# The survival function at three follow-up times, with delta-method
# confidence intervals. Observed times run from 5 to 225 months.
survival_predictions(
  times = c(50, 100, 150),
  theta = coef(m),
  covariance = vcov(m),
  distribution = "weibull",
  measure = "survival"
)
\dontshow{\}) # examplesIf}
}
