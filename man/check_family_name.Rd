% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{check_family_name}
\alias{check_family_name}
\title{Check that a distribution or link is a single string}
\usage{
check_family_name(value, arg, call = rlang::caller_env())
}
\arguments{
\item{value}{The distribution or link supplied by the caller.}

\item{arg}{The argument name, used in the error message.}

\item{call}{The frame to report the refusal against, which is the estimating
equation the caller wrote.}
}
\value{
Invisible \code{NULL}. Raises an error if the value is not a single
non-missing string.
}
\description{
The generalized linear estimating equations read \code{distribution} and \code{link}
by name, and the helpers that read them dispatch through \code{if} on a comparison
with a single value. A \code{NULL} or a vector longer than one never reaches the
unsupported-name diagnostic those helpers raise: it fails the \code{if} first, as
base R's \verb{argument is of length zero} or \verb{the condition has length > 1},
reported against a branch the caller never wrote. \code{ee_glm()} partitions
\code{theta} on a comparison of its own before either helper is reached, so the
name is judged where the caller supplied it instead.
}
\keyword{internal}
