% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-causal.R
\name{ee_gestimation_snmm}
\alias{ee_gestimation_snmm}
\title{Estimating equations for g-estimation of structural nested mean models}
\usage{
ee_gestimation_snmm(
  theta,
  y,
  A,
  W,
  V,
  X = NULL,
  model = "linear",
  weights = NULL
)
}
\arguments{
\item{theta}{Numeric vector. For the inefficient g-estimator,
length is \code{b + c} (SMM parameters + PS model parameters). For the
efficient g-estimator, length is \code{b + c + d} (SMM + PS + outcome
model parameters).}

\item{y}{Numeric vector of n observed outcomes.}

\item{A}{Numeric vector of n binary treatment indicators (0/1).}

\item{W}{Numeric n-by-c design matrix for the propensity score model.}

\item{V}{Numeric n-by-b design matrix for the structural mean model.
Should NOT include A itself.}

\item{X}{Optional n-by-d design matrix for the outcome model
(efficient g-estimator). Default \code{NULL} (inefficient g-estimator).}

\item{model}{Character string: \code{"linear"} or \code{"poisson"}. Default
\code{"linear"}.}

\item{weights}{Optional numeric vector of n weights. Default \code{NULL}.}
}
\value{
A matrix of estimating equation contributions.
}
\description{
Estimates the parameters of a structural nested mean model via
g-estimation. Supports both inefficient (X = NULL) and efficient
(X provided) g-estimators, and both linear and log-linear (Poisson)
structural mean models.
}
