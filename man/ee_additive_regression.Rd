% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-regression.R
\name{ee_additive_regression}
\alias{ee_additive_regression}
\title{Estimating equation for additive regression (GAM)}
\usage{
ee_additive_regression(
  theta,
  X,
  y,
  specifications,
  model,
  weights = NULL,
  offset = NULL
)
}
\arguments{
\item{theta}{Numeric vector of length equal to the number of columns in
the expanded additive design matrix.}

\item{X}{Numeric n-by-b design matrix (before spline expansion).}

\item{y}{Numeric vector of n observed outcome values.}

\item{specifications}{A list of length \code{b} controlling spline generation.
Each element is either \code{NULL} (no spline) or a list with keys \code{knots},
and optionally \code{natural}, \code{power}, \code{penalty}, \code{normalized}. See
\code{\link[=additive_design_matrix]{additive_design_matrix()}} for details.}

\item{model}{Character string: \code{"linear"}, \code{"logistic"}, or \code{"poisson"}.}

\item{weights}{Optional numeric vector of n weights. Default \code{NULL}.}

\item{offset}{Optional numeric vector of n offsets. Default \code{NULL}.}
}
\value{
A p-by-n matrix, where p is the number of columns in the expanded
additive design matrix.
}
\description{
Generalized Additive Model via L2-penalized splines. Internally expands \code{X}
using \code{\link[=additive_design_matrix]{additive_design_matrix()}} and delegates to \code{\link[=ee_bridge_regression]{ee_bridge_regression()}}
with \code{gamma = 2} (ridge penalty). The penalty only applies to the spline
basis terms, not to the original linear terms.
}
\examples{
set.seed(42)
n <- 200
x <- runif(n, -3, 3)
y <- sin(x) + rnorm(n, sd = 0.3)
X <- cbind(1, x)

# No spline on the intercept column, a penalized spline on x.
specs <- list(NULL, list(knots = c(-2, -1, 0, 1, 2), penalty = 5))

psi <- function(theta) {
  ee_additive_regression(
    theta,
    X = X,
    y = y,
    specifications = specs,
    model = "linear"
  )
}

# One parameter per column of the expanded design matrix.
m <- m_estimate(
  stacked_equations = psi,
  init = rep(0, ncol(additive_design_matrix(X, specs)))
)
coef(m)

}
