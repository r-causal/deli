% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{eval_psi_at_init}
\alias{eval_psi_at_init}
\title{Evaluate and validate the estimating function at the initial values}
\usage{
eval_psi_at_init(
  psi,
  init,
  allow_over_identification = FALSE,
  error_call = NULL
)
}
\arguments{
\item{psi}{The estimating-function closure.}

\item{init}{The initial parameter vector.}

\item{allow_over_identification}{Logical. When \code{TRUE} (the GMM case), the
estimating function may return more equations than parameters, so only a
shortfall is rejected. Default \code{FALSE}.}

\item{error_call}{The frame to report a failure at the caller's own \code{init}
against, which is the \code{\link[=estimate]{estimate()}} method the caller reached. At an \code{init}
the formula interface generated, the entry point recorded on the closure is
preferred to it.}
}
\value{
The value of \code{psi(init)}. Raises an error if it is not a valid
estimating-function return. A failure reframed as a problem with the
automatic length carries the class \code{deli_formula_auto_init_error}, so a
caller can recognize it without matching the message.
}
\description{
Performs the single evaluation of the estimating function at \code{init} that
\code{\link[=estimate]{estimate()}} needs, and validates the return with \code{\link[=check_psi_at_init]{check_psi_at_init()}}.
}
\details{
When the formula interface generated \code{init} itself it records the vector on
the closure as the \code{deli_auto_init} attribute, and this reframes a failure at
exactly those starting values against the automatic \code{init}, which the user
never chose. Some estimating equations append parameters beyond the
regression coefficients (for example \code{\link[=ee_glm]{ee_glm()}} with \code{"gamma"} or
\code{"negative_binomial"}, which add a scale or dispersion parameter), so the
automatic \code{init} is one short and the estimating function either fails
outright or returns the wrong number of rows. Where the equation is one the
formula interface recognizes, the reframed message names the parameter the
automatic length leaves out; otherwise how much the message can claim about
the length depends on the failure, which \code{abort_formula_auto_init()}
documents.

Only those two failures are reframed. A \code{NULL} or non-numeric return keeps
its own message, because an \code{init} one element short cannot cause either. A
non-finite return keeps its own message when the number of rows fits the
automatic length, and is reframed when it does not, because an estimating
function reading a parameter the automatic \code{init} does not reach returns
\code{NA}s and the wrong number of rows together, and the row count is the more
accurate of the two. Every failure raised after this point, in the solver or
the sandwich components, keeps its own message as well.
}
\keyword{internal}
