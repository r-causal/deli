% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-causal.R
\name{ee_ipw_msm}
\alias{ee_ipw_msm}
\title{Estimating equations for IPW marginal structural model}
\usage{
ee_ipw_msm(
  theta,
  y,
  A,
  W,
  V,
  distribution,
  link,
  hyperparameter = NULL,
  truncate = NULL,
  weights = NULL
)
}
\arguments{
\item{theta}{Numeric vector of length \code{c + b}, where \code{c} is the number
of MSM parameters and \code{b} is the number of propensity score model
parameters.}

\item{y}{Numeric vector of n observed outcomes.}

\item{A}{Numeric vector of n binary treatment indicators (0/1).}

\item{W}{Numeric n-by-b design matrix for the propensity score model.}

\item{V}{Numeric n-by-c design matrix for the marginal structural model.}

\item{distribution}{Character string for the GLM distribution of the outcome
model. Every name \code{\link[=ee_glm]{ee_glm()}} takes except the three that estimate a
nuisance parameter, which the \code{theta} partition here reserves no slot for:
\code{"normal"} (or \code{"gaussian"}), \code{"binomial"} (or \code{"bernoulli"}, or \code{"bin"}),
\code{"poisson"}, \code{"inverse_normal"} (or \code{"inverse_gaussian"}), and
\code{"tweedie"}. See \code{hyperparameter} for what naming one of the three does.}

\item{link}{Character string for the GLM link function. See \code{\link[=ee_glm]{ee_glm()}}
for options.}

\item{hyperparameter}{Optional numeric hyperparameter passed straight
through to the marginal structural model's \code{\link[=ee_glm]{ee_glm()}} call. Used by the
tweedie outcome distribution, where it fixes the variance power
\code{v(mu) = mu^hyperparameter}. Default \code{NULL}. Note that the theta
partition reserves exactly \code{ncol(V)} slots for the MSM and no slot for
an estimated nuisance parameter, so outcome families that carry one
(\code{"gamma"}, \code{"negative_binomial"}, \code{"nb"}) cannot be used as the MSM
outcome model. Naming one is refused by name, before the outcome model is
formed and whatever \code{hyperparameter} is set to. Python Delicatessen cannot
fit those families either, where the attempt fails as a shape error.}

\item{truncate}{Optional length-2 numeric vector \code{c(lower, upper)} to
clip estimated propensity scores. Bounds must be in ascending order
(\code{lower <= upper}). Default \code{NULL}.}

\item{weights}{Optional numeric vector of n weights. Default \code{NULL}.}
}
\value{
A \code{(c+b)}-by-n matrix of estimating equation contributions. The
marginal structural model rows are named \verb{MSM alpha_0} through
\verb{MSM alpha_(c-1)}, matching the zero-based subscripts the literature gives
those parameters. The propensity score rows are named \code{W_1} through \code{W_b}
for the columns of \code{W}.
}
\description{
Estimates the parameters of a marginal structural model using inverse
probability weighting with a logistic propensity score model.
}
\examples{
# A confounded binary treatment whose true effect on the outcome is -2.
set.seed(42)
n <- 500
W <- rbinom(n, 1, 0.5)
A <- rbinom(n, 1, 0.25 + 0.5 * W)
Y <- 5 + 2 * W - 2 * A + rnorm(n)

W_ps <- cbind(1, W) # Propensity score design matrix
V <- cbind(1, A) # Marginal structural model design matrix

psi <- function(theta) {
  ee_ipw_msm(
    theta,
    y = Y,
    A = A,
    W = W_ps,
    V = V,
    distribution = "normal",
    link = "identity"
  )
}

# theta holds the two marginal structural model coefficients, whose slope is
# the causal effect, followed by the two propensity score coefficients.
m <- m_estimate(stacked_equations = psi, init = rep(0, 4))
coef(m)

}
