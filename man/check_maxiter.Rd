% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{check_maxiter}
\alias{check_maxiter}
\title{Check that an iteration budget is a single positive whole number}
\usage{
check_maxiter(maxiter, call = rlang::caller_env())
}
\arguments{
\item{maxiter}{The iteration budget supplied by the caller.}

\item{call}{The frame to report the refusal against, on the same terms as
\code{\link[=check_dx]{check_dx()}}'s.}
}
\value{
Invisible \code{NULL}. Raises an error if the budget is not a single
positive whole number.
}
\description{
Validates \code{maxiter}, the number of iterations a solver is allowed. Nothing
downstream reports a budget that cannot be counted with, and each solver
mishandles a bad one in its own way. A vector budget reached
\code{\link[rootSolve:multiroot]{rootSolve::multiroot()}}, whose own guard tests it with an \code{if} and failed
with \verb{the condition has length > 1} against an expression no caller wrote,
and where the budget survived to be reported the non-convergence message
pluralized on the length of the vector rather than on the budget. A budget
that is not a number failed as \verb{'maxiter' must be numeric} from the same
place. A budget below one asks for a solve with no iterations in it.
}
\details{
Judged where it is supplied, on the same terms as \code{\link[=check_dx]{check_dx()}}, so a budget
that cannot be used is reported whether or not the solver this call reaches
would have looked at it.
}
\keyword{internal}
