% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-causal.R
\name{ee_mean_sensitivity_analysis}
\alias{ee_mean_sensitivity_analysis}
\title{Estimating equations for weighted sensitivity analysis of the mean}
\usage{
ee_mean_sensitivity_analysis(theta, y, delta, X, q_eval, H_function)
}
\arguments{
\item{theta}{Numeric vector of length \code{1 + b}, where \code{b} is the
number of columns in \code{X}. The first element is the corrected
mean; the remainder are regression coefficients.}

\item{y}{Numeric vector of n outcome values. Missing values should
be indicated via the \code{delta} parameter.}

\item{delta}{Numeric vector of n indicators: 1 if \code{y} is observed,
0 if missing. Must not contain \code{NA}.}

\item{X}{Numeric n-by-b design matrix for the missingness model.
Should include an intercept column. Must not contain \code{NA}.}

\item{q_eval}{Numeric vector of n evaluated sensitivity function
values, i.e. \code{q(Y, alpha)}.}

\item{H_function}{A function mapping real values to \verb{[0, 1]} that
is monotone increasing (e.g., \code{\link[=inverse_logit]{inverse_logit()}}).}
}
\value{
A \code{(1+b)}-by-n matrix of estimating equation contributions, with the
first row named \code{corrected_mean} and the missingness model rows named \code{X_1}
through \code{X_b} for the columns of \code{X}.
}
\description{
Estimates the mean of an outcome with missing data using a weighted
sensitivity analysis approach. Handles MCAR, MAR, and MNAR
mechanisms by specifying a user-defined sensitivity function \code{q_eval}
and a monotone increasing distribution function \code{H_function}.
}
\examples{
# An outcome observed for only part of the sample, with missingness driven by
# the measured covariate W.
set.seed(42)
n <- 500
W <- rbinom(n, 1, 0.5)
Y_full <- 200 - 35 * W + rnorm(n, sd = 5)
delta <- rbinom(n, 1, plogis(2 + W))

# Missing outcomes never enter the estimating equation, so any placeholder
# value works; a zero keeps the arithmetic finite.
Y <- ifelse(delta == 1, Y_full, 0)
X <- cbind(1, W) # Missingness model design matrix

# A sensitivity function of zero everywhere assumes the outcome is missing at
# random given W. Nonzero values encode departures from that assumption.
psi <- function(theta) {
  ee_mean_sensitivity_analysis(
    theta,
    y = Y,
    delta = delta,
    X = X,
    q_eval = rep(0, n),
    H_function = inverse_logit
  )
}

# theta holds the corrected mean, started near the observed outcomes,
# followed by the two missingness model coefficients.
m <- m_estimate(stacked_equations = psi, init = c(180, 0, 0))
coef(m)

}
