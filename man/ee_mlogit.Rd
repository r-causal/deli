% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-regression.R
\name{ee_mlogit}
\alias{ee_mlogit}
\title{Estimating equation for multinomial logistic regression}
\usage{
ee_mlogit(theta, X, y, weights = NULL, offset = NULL)
}
\arguments{
\item{theta}{Numeric vector of length \code{b * (k-1)}.}

\item{X}{Numeric n-by-b design matrix.}

\item{y}{Numeric n-by-k indicator matrix (first column = reference), with k
at least three.}

\item{weights}{Optional numeric vector of n weights. Default \code{NULL}.}

\item{offset}{Optional numeric vector of n offsets. Default \code{NULL}.}
}
\value{
A \code{(b * (k-1))}-by-n matrix.
}
\description{
Supports unranked categorical outcomes. \code{y} must be an n-by-k indicator
matrix where the first column is the reference category, and k must be at
least three: a two-level outcome is logistic regression, which \code{\link[=ee_glm]{ee_glm()}}
fits with \code{distribution = "binomial"}. Returns a \code{(b * (k-1))}-by-n matrix.
}
\examples{
set.seed(123)
n <- 50
W <- rbinom(n, 1, 0.5)
probs <- cbind(0.5 - 0.2 * W, 0.3 + 0.1 * W, 0.2 + 0.1 * W)
y_cat <- sapply(seq_len(n), function(i) sample(1:3, 1, prob = probs[i, ]))

# The outcome is an indicator matrix whose first column is the reference
# category.
y <- cbind(
  as.integer(y_cat == 1),
  as.integer(y_cat == 2),
  as.integer(y_cat == 3)
)
X <- cbind(1, W)

psi <- function(theta) ee_mlogit(theta, X = X, y = y)

# Two columns of X and two non-reference categories give four parameters.
m <- m_estimate(stacked_equations = psi, init = rep(0, 4))
coef(m)

}
