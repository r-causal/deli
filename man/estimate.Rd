% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{estimate}
\alias{estimate}
\title{Estimate parameters and sandwich variance}
\usage{
estimate(
  object,
  solver = NULL,
  maxiter = 5000,
  tolerance = 1e-09,
  deriv_method = "capprox",
  dx = 1e-09,
  allow_pinv = TRUE,
  ...
)

## S7 method for class <deli::GMMEstimator>
estimate(
  object,
  solver = NULL,
  maxiter = 5000,
  tolerance = 1e-09,
  deriv_method = "capprox",
  dx = 1e-09,
  allow_pinv = TRUE,
  ...
)
}
\arguments{
\item{object}{An \code{MEstimator} or \code{GMMEstimator} object.}

\item{solver}{Character string specifying the solver algorithm, or a custom
function. When \code{NULL} (default), uses \code{"rootSolve"} for \code{MEstimator}
(\code{\link[rootSolve:multiroot]{rootSolve::multiroot()}}) and \code{"BFGS"} for \code{GMMEstimator}
(\code{\link[stats:optim]{stats::optim()}}). Other options for \code{MEstimator}: \code{"lm"}, the
Levenberg-Marquardt algorithm (\code{\link[minpack.lm:nls.lm]{minpack.lm::nls.lm()}}), which mirrors the
default solver of Python \code{delicatessen}
(\code{scipy.optimize.root(method = "lm")}); and \code{"nleqslv"} (uses
\code{\link[nleqslv:nleqslv]{nleqslv::nleqslv()}}). A custom function must accept
\code{stacked_equations} and \code{init} arguments and return the solved theta
vector. Whichever solver is used, the point it returns is judged against
the estimating equations themselves and a warning is raised when they are
not solved there. A custom function reports no status of its own, so its
point is judged exactly as a built-in solver's is. Two \code{GMMEstimator}
fits are exceptions: an over-identified one cannot drive every moment to
zero, and a \code{subset} one holds some parameters fixed while still summing
every equation into the objective, so neither is judged either way and
neither warns. For those, inspect \code{rowSums()} of the estimating functions
at the returned values.}

\item{maxiter}{Integer maximum iterations for the solver (default 5000).}

\item{tolerance}{Numeric tolerance for the solver (default 1e-9).}

\item{deriv_method}{Character string for the derivative method used to
compute the bread matrix. One of \code{"capprox"} (central difference),
\code{"fapprox"} (forward difference), \code{"bapprox"} (backward difference), or
\code{"exact"} (forward-mode automatic differentiation). Default \code{"capprox"}.
Exact differentiation removes the finite-difference step size but requires
that the estimating equation is composed of operations the autodiff
supports (see \code{\link[=auto_differentiation]{auto_differentiation()}}). For post-estimation transforms and
the survival prediction helpers, exact differentiation is also available
through \code{\link[=delta_method]{delta_method()}}.}

\item{dx}{Numeric step size for numerical differentiation; ignored when
\code{deriv_method = "exact"} (default 1e-9). The step is absolute and is
floored at the floating-point resolution of each estimate, so a large
parameter magnitude cannot silently reduce it to nothing; see
\code{\link[=approx_differentiation]{approx_differentiation()}}.}

\item{allow_pinv}{Logical. Use pseudo-inverse if bread is singular? Default
\code{TRUE}.}

\item{...}{Not used. Must be empty, so a name that is not one of the
documented arguments is an error rather than silently ignored.}
}
\value{
A modified \code{MEstimator} object with populated \code{theta}, \code{bread},
\code{meat}, \code{variance}, and \code{asymptotic_variance} properties.
}
\description{
Solves the estimating equations for the parameter vector \code{theta} and
computes the empirical sandwich variance estimator.
}
\details{
The estimates and every matrix built from them carry parameter names, taken
from the first of two channels that supplies them. Names on \code{init} come
first: a parameter the caller named keeps that name, and one left unnamed is
numbered by position, so \code{init = c(mu = 0, 1)} gives \code{c("mu", "theta_2")}.
Where \code{init} carries no names at all, the row names of the estimating
functions are read instead. That is how a \code{stacked_equations} function names
the parameters it defines, since the estimator otherwise sees only an opaque
closure returning a matrix. The formula interface always names \code{init} from
the model matrix columns, so the row names matter to the function interface.

Row names are read only when they label every parameter distinctly: one name
per parameter, none empty, none missing, and no two alike. An incomplete or
repetitive set is discarded rather than patched up, because it is usually an
accident of how the stack was built. \code{rbind()} pads the rows of an unlabeled
block with empty strings, and \code{t(X * resid)} on a design whose intercept
column has no name produces the same shape; \code{rbind()} also names each row
after the variable that supplied it, so two blocks that each begin with a
variable of the same name repeat a label. The count has to match as well,
which is what keeps an over-identified \code{GMMEstimator} out: its rows are
moment conditions and outnumber the parameters, so their labels describe the
equations. Where neither channel applies, the parameters are numbered
\code{theta_1} through \code{theta_p}.

Row names survive exact differentiation. Assigning them is ignored while a
value carries derivatives, since the labels are read from the plain
evaluation at the solved values, and deli registers the setter as an S3
method so that an estimating function written anywhere reaches it.
}
\examples{
psi <- function(theta) {
  y <- c(1, 2, 3, 4, 5)
  matrix(y - theta[1], nrow = 1)
}
m <- MEstimator(stacked_equations = psi, init = c(0)) |>
  estimate()
m@theta
}
