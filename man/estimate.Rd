% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{estimate}
\alias{estimate}
\title{Estimate parameters and sandwich variance}
\usage{
estimate(
  object,
  solver = NULL,
  maxiter = 5000,
  tolerance = 1e-09,
  deriv_method = "capprox",
  dx = 1e-09,
  allow_pinv = TRUE,
  ...
)

## S7 method for class <deli::GMMEstimator>
estimate(
  object,
  solver = NULL,
  maxiter = 5000,
  tolerance = 1e-09,
  deriv_method = "capprox",
  dx = 1e-09,
  allow_pinv = TRUE,
  ...
)
}
\arguments{
\item{object}{An \code{MEstimator} object.}

\item{solver}{Character string specifying the solver algorithm, or a custom
function. When \code{NULL} (default), uses \code{"rootSolve"} for \code{MEstimator}
(\code{\link[rootSolve:multiroot]{rootSolve::multiroot()}}) and \code{"BFGS"} for \code{GMMEstimator}
(\code{\link[stats:optim]{stats::optim()}}). Other options for \code{MEstimator}: \code{"lm"}, the
Levenberg-Marquardt algorithm (\code{\link[minpack.lm:nls.lm]{minpack.lm::nls.lm()}}), which mirrors the
default solver of Python \code{delicatessen}
(\code{scipy.optimize.root(method = "lm")}); and \code{"nleqslv"} (uses
\code{\link[nleqslv:nleqslv]{nleqslv::nleqslv()}}). A custom function must accept
\code{stacked_equations} and \code{init} arguments and return the solved theta
vector.}

\item{maxiter}{Integer maximum iterations for the solver (default 5000).}

\item{tolerance}{Numeric tolerance for the solver (default 1e-9).}

\item{deriv_method}{Character string for the derivative method used to
compute the bread matrix. One of \code{"capprox"} (central difference),
\code{"fapprox"} (forward difference), \code{"bapprox"} (backward difference), or
\code{"exact"} (forward-mode automatic differentiation). Default \code{"capprox"}.
Exact differentiation removes the finite-difference step size but requires
that the estimating equation is composed of operations the autodiff
supports (see \code{\link[=auto_differentiation]{auto_differentiation()}}). For post-estimation transforms and
the survival prediction helpers, exact differentiation is also available
through \code{\link[=delta_method]{delta_method()}}.}

\item{dx}{Numeric step size for numerical differentiation; ignored when
\code{deriv_method = "exact"} (default 1e-9).}

\item{allow_pinv}{Logical. Use pseudo-inverse if bread is singular? Default
\code{TRUE}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A modified \code{MEstimator} object with populated \code{theta}, \code{bread},
\code{meat}, \code{variance}, and \code{asymptotic_variance} properties.
}
\description{
Solves the estimating equations for the parameter vector \code{theta} and
computes the empirical sandwich variance estimator.
}
\examples{
psi <- function(theta) {
  y <- c(1, 2, 3, 4, 5)
  matrix(y - theta[1], nrow = 1)
}
m <- MEstimator(stacked_equations = psi, init = c(0)) |>
  estimate()
m@theta
}
