% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ee-causal.R
\name{ee_aipw}
\alias{ee_aipw}
\title{Estimating equations for augmented inverse probability weighting (AIPW)}
\usage{
ee_aipw(theta, y, A, W, X, X1, X0, truncate = NULL, force_continuous = FALSE)
}
\arguments{
\item{theta}{Numeric vector of length \code{3 + b + c}, where \code{b} is the
number of propensity score model parameters and \code{c} is the number of
outcome model parameters.}

\item{y}{Numeric vector of n observed outcomes.}

\item{A}{Numeric vector of n binary treatment indicators (0/1).}

\item{W}{Numeric n-by-b design matrix for the propensity score model.}

\item{X}{Numeric n-by-c design matrix for the outcome model.}

\item{X1}{Numeric n-by-c design matrix under A=1 for all units.}

\item{X0}{Numeric n-by-c design matrix under A=0 for all units.}

\item{truncate}{Optional length-2 numeric vector \code{c(lower, upper)} to
clip propensity scores. Bounds must be in ascending order
(\code{lower <= upper}). Default \code{NULL}.}

\item{force_continuous}{Logical. Force linear regression for outcome
model? Default \code{FALSE}.}
}
\value{
A \code{(3+b+c)}-by-n matrix of estimating equation contributions.
}
\description{
Estimates the average causal effect using AIPW, which combines a
propensity score model and an outcome model for doubly-robust estimation.
}
\examples{
# A binary treatment, two confounders, and a continuous outcome whose true
# average causal effect is 1.5.
set.seed(42)
n <- 1000
W1 <- rnorm(n)
W2 <- rbinom(n, 1, 0.4)
A <- rbinom(n, 1, inverse_logit(-0.5 + 0.5 * W1 + 0.3 * W2))
Y <- 2 + 1.5 * A + W1 - 0.5 * W2 + rnorm(n)

W_ps <- cbind(1, W1, W2) # Propensity score design matrix
X <- cbind(1, A, W1, W2) # Outcome model, observed design matrix
X1 <- cbind(1, 1, W1, W2) # Outcome model, everyone treated
X0 <- cbind(1, 0, W1, W2) # Outcome model, everyone untreated

psi <- function(theta) {
  ee_aipw(theta, y = Y, A = A, W = W_ps, X = X, X1 = X1, X0 = X0)
}

# theta holds the average causal effect, the mean under treatment, and the
# mean under no treatment, followed by the three propensity score
# coefficients and the four outcome model coefficients.
m <- m_estimate(stacked_equations = psi, init = rep(0, 10))
summary(m, subset = 1:3)

}
