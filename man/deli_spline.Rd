% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline.R
\name{deli_spline}
\alias{deli_spline}
\title{Generate polynomial spline basis terms}
\usage{
deli_spline(x, knots, power = 3, restricted = TRUE, normalized = FALSE)
}
\arguments{
\item{x}{Numeric vector of observed values.}

\item{knots}{Numeric vector of knot locations. Should be between the
min and max of \code{x}.}

\item{power}{Numeric power for the spline terms. Default \code{3} (cubic).}

\item{restricted}{Logical. If \code{TRUE} (default), generate restricted
(natural) splines. If \code{FALSE}, generate unrestricted splines.}

\item{normalized}{Logical. If \code{TRUE}, divide spline terms by the range
of knots raised to \code{power}. Default \code{FALSE}.}
}
\value{
A matrix with \code{length(x)} rows. Number of columns is
\code{length(knots)} for unrestricted or \code{length(knots) - 1} for restricted.
}
\description{
Generates polynomial spline terms for a numeric vector at pre-specified
knot locations. Default is restricted (natural) cubic splines, but
unrestricted splines with different polynomial terms can also be generated.
}
\details{
Unrestricted splines for knot \eqn{k} are:
\deqn{s_k(X) = I(X > k) (X - k)^a}

Restricted (natural) splines subtract the last spline term:
\deqn{r_k(X) = s_k(X) - s_K(X)}
where \eqn{K} is the largest knot. Restricted splines return one fewer
column than the number of knots.
}
\examples{
# Restricted quadratic splines at four knots, so three basis columns
s <- deli_spline(1:59, knots = c(10, 20, 30, 40), power = 2)
dim(s)

# Each term stays at zero below its knot and grows above it
s[c(5, 15, 25, 35, 45, 55), ]

}
