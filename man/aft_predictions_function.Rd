% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictions.R
\name{aft_predictions_function}
\alias{aft_predictions_function}
\title{Function-level predicted survival measures from an AFT model}
\usage{
aft_predictions_function(
  X,
  times,
  theta,
  covariance,
  distribution,
  measure = "survival",
  alpha = 0.05,
  deriv_method = "capprox",
  dx = 1e-09
)
}
\arguments{
\item{X}{Numeric 1-by-b design matrix giving a single covariate pattern.
More than one row is an error, since each pattern has its own variance.}

\item{times}{Numeric vector of time points for prediction.}

\item{theta}{Numeric vector of estimated parameters from \code{ee_aft}.}

\item{covariance}{Numeric covariance matrix from \code{vcov(m)}.}

\item{distribution}{Character string matching the distribution used in
\code{ee_aft}.}

\item{measure}{Character string: \code{"survival"}, \code{"risk"}, \code{"density"},
\code{"hazard"}, or \code{"cumulative_hazard"}. Default \code{"survival"}.}

\item{alpha}{Numeric significance level. Default \code{0.05} (95\% CIs).}

\item{deriv_method}{Character string for the derivative method used to build
the delta-method Jacobian. One of \code{"capprox"} (central difference),
\code{"fapprox"} (forward difference), \code{"bapprox"} (backward difference), or
\code{"exact"} (forward-mode automatic differentiation). Default \code{"capprox"}.
Python Delicatessen uses exact differentiation internally; pass
\code{deriv_method = "exact"} to reproduce it with exact derivatives and no
step-size tuning. See \code{\link[=delta_method]{delta_method()}}.}

\item{dx}{Numeric step size for the finite-difference methods; ignored when
\code{deriv_method = "exact"}. Default \code{1e-9}. The step is absolute and is
floored at the floating-point resolution of each estimate, so a large
parameter magnitude cannot silently reduce it to nothing; see
\code{\link[=approx_differentiation]{approx_differentiation()}}.}
}
\value{
A data frame with one row per time point and columns: \code{time},
\code{predicted}, \code{variance}, \code{lower}, \code{upper}.
}
\description{
Computes predicted survival analysis measures and point-wise confidence
intervals from an accelerated failure time model for a single covariate
pattern across a set of time points. The point estimates mirror
\code{\link[=aft_predictions_individual]{aft_predictions_individual()}}; the variance is obtained with the delta
method (see \code{\link[=delta_method]{delta_method()}}) and Wald-type intervals are formed on the
resulting standard errors. Meant to be used after fitting \code{\link[=ee_aft]{ee_aft()}} with
\code{\link[=MEstimator]{MEstimator()}}, typically to draw a measure and its confidence band over
time.
}
\details{
The survival and hazard for a covariate pattern \eqn{X} are

\deqn{S(t) = S_{\epsilon}\left( \frac{\log(t) - X \beta^T}{\sigma} \right)}
\deqn{h(t) = (\sigma t)^{-1} h_{\epsilon}\left(
  \frac{\log(t) - X \beta^T}{\sigma} \right)}

where \eqn{S_{\epsilon}} and \eqn{h_{\epsilon}} are the error survival and
hazard functions for the chosen \code{distribution}. The requested \code{measure} is
derived from these through \code{\link[=convert_survival_measures]{convert_survival_measures()}}.
}
\section{Length-one times}{

A single time point is supported and returns a one-row data frame equal to
the corresponding row of a multi-time call. This is a deliberate
improvement over Python Delicatessen, whose \code{aft_predictions_function}
raises on a scalar or length-one \code{times} because it takes the diagonal of a
scalar delta-method covariance.
}

\examples{
\dontshow{if (requireNamespace("nleqslv", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# Weibull AFT fit, then a survival curve for one covariate pattern
set.seed(1)
n <- 200
x <- rbinom(n, 1, 0.5)
Xd <- cbind(1, x)
eps <- log(-log(runif(n)))
t_event <- exp(2 + 0.5 * x + 0.8 * eps)
t_censor <- rexp(n, rate = 0.02)
t_obs <- pmin(t_event, t_censor)
delta <- as.numeric(t_event <= t_censor)

psi <- function(theta) {
  ee_aft(theta, X = Xd, time = t_obs, event = delta, distribution = "weibull")
}
m <- m_estimate(
  stacked_equations = psi,
  init = c(mean(log(t_obs)), 0, 0),
  solver = "nleqslv"
)

aft_predictions_function(
  X = matrix(c(1, 1), nrow = 1), times = c(5, 10, 20),
  theta = coef(m), covariance = vcov(m),
  distribution = "weibull", measure = "risk"
)
\dontshow{\}) # examplesIf}
}
